# Pinned for reproducibility. Rebuild artifacts after any version change.
#
# Python 3.12 or newer. The floor is not a preference: scipy 1.18.0 requires
# >=3.12, and every other pin here requires 3.11 or less, so 3.12 is the oldest
# interpreter this set installs on. Tested on 3.14.0, which is what .python-version
# and the Dockerfile ask deployments for; tests/test_deployment_config.py checks
# those three keep agreeing.
pandas==3.0.0
numpy==2.3.5
scikit-learn==1.9.0
scipy==1.18.0
matplotlib==3.10.8
seaborn==0.13.2
plotly==6.5.2
streamlit==1.62.0
joblib==1.5.3
pytest==8.4.2
