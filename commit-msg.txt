feat(companion): Sunee behaviour system - intro, cursor gaze, emotion ladder, nav reactions

- intro bubble ~3s after load, once per session, dismissable, returns to idle
- cursor gaze approximated via lab glance expressions (lab has no pointer/gaze API; documented in companionConfig.js)
- inactivity ladder idle > bored (30s) > impatient (60s) > angry (2min): animation-only, gradual, resets with happy/surprised
- page-navigation reactions with summaries drawn from real band copy; priority bubble manager (nav > intro > manual > fact > inactivity), one bubble at a time, close x on every bubble
- random facts every 45-90s of active use, never right after a nav summary; 3 season facts added from seasonal.json exports
- sunee.avatar.json grows to 21 lab-verbatim expressions and 13 lab-verbatim sequences (bored/suspicious/angry/listening/excited); analysisData.js mirrors peakHourBySeason

Co-Authored-By: Claude Code <noreply@anthropic.com>