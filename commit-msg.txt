feat(companion): Sunee mascot with fact bubbles; AGPL avatar runtime vendored

- web/src/vendor/bible-strong/: runtime vendored from the
  bible-strong-avatar-lab repo (@bible-strong/avatar-core: geometry,
  surfaces, body, scene, ambient-motion, playback state machine + the
  Avatar renderer), TypeScript stripped, ajv replaced by a dependency-free
  validation shim. LICENSE (verbatim AGPL-3.0) and NOTICE.md (provenance,
  section-13 consequence) included.
- web/src/companion/sunee.avatar.json: Sunee's body, 14 expressions and 8
  animations (idle, talking, happy, curious, proud, surprised, shy, sleepy)
  transcribed verbatim from the lab's studio library.
- web/src/companion/projectFacts.js: 14 facts derived live from
  web/src/analysisData.js (1,752,000 records, PCA 10 components at 95.0%,
  K=4 composite 0.944, cluster sizes, stability/recovery ARI, seasonal,
  longitudinal, SHAP and real-world results when available). No invented
  statistics; a fact silently drops out if its data is unavailable.
- web/src/companion/ProjectCompanion.jsx + companion.css: bottom-right
  mascot that idles, pops a fact every 30-60s (interval doubled under
  prefers-reduced-motion), animates while the bubble shows, then returns
  to idle. Click for an instant fact (10s cooldown). Hidden-tab
  suppression, role=status bubble, keyboard-accessible button; error
  boundary renders null on failure so the dashboard is never affected.
- web/src/main.jsx: import + ENABLE_PROJECT_COMPANION flag + mount beside
  <App />; the dashboard itself is untouched.

The combined web app must be distributed under AGPL-3.0 while the
companion is present. Deleting web/src/companion/ and
web/src/vendor/bible-strong/ and reverting the main.jsx edit restores the
pure-MIT project.

Co-Authored-By: Claude Code <noreply@anthropic.com>